# Model Artifacts

Place detector and classifier model files here during local development or device deployment.

Do not commit large model artifacts to the repository. Document model name, version, input size, labels, conversion command, and Orange Pi AI Pro performance results here when a model is selected.
