service:
  host: "0.0.0.0"
  port: 8080

logging:
  backend: "syslog"
  level: "info"
  ident: "video_sentinel"

inference:
  backend: "ascendcl"
  model_path: "models/yolo/yolo26n.om"
  device_id: 0

runtime:
  data_dir: "/var/lib/video-sentinel"

pipeline:
  max_frames: 300
